<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ict.interviewdotboot.mapper.OpenaiMapper">
    <update id="introduceUpdate" parameterType="ResumeVO">
        update Resume_t
        set content=#{content}
        where resume_idx=#{resume_idx}
    </update>

    <update id="update1" parameterType="MyuserVO">
        update Add_profile
        set p_job = #{p_job},
            p_class = #{p_class},
            p_career = #{p_career},
            p_location = #{p_location}
        where u_idx = #{u_idx};
    </update>

    <update id="update2" parameterType="MyuserVO">
        update User_t
        set Field = #{Field}
        where u_idx = #{u_idx};
    </update>

    <insert id="insert" parameterType="ResumeVO">
        insert into Resume_t(u_idx, title, content, location, job, classInfo, career)
        values(#{u_idx}, #{title}, #{content}, #{location}, #{job}, #{classInfo}, #{career})
    </insert>

    <select id="re_select" parameterType="String" resultType="ResumeVO">
        select *
        from Resume_t
        where u_idx = #{u_idx}
        order by resume_idx desc
    </select>

    <select id="re_select2" parameterType="String" resultType="ResumeVO">
        select *
        from Resume_t
        where resume_idx = #{resume_idx}
    </select>

    <update id="re_update" parameterType="ResumeVO">
        update Resume_t
        set title=#{title}, content=#{content}, location=#{location}, job=#{job}, classInfo=#{classInfo}, career=#{career}
        where resume_idx=#{resume_idx}
    </update>

    <delete id="re_delete" parameterType="ResumeVO">
        delete from Resume_t
        where resume_idx=#{resume_idx}
    </delete>
</mapper>
