<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ict.interviewdotboot.mapper.TossMapper">
    <insert id="savePayment" parameterType="TossVO">
        INSERT INTO Toss_t(order_id, order_name, amount, paymentkey)
        VALUES(#{order_id}, #{order_name}, #{amount}, #{paymentkey})
    </insert>
</mapper>
