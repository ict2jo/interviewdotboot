<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ict.interviewdotboot.mapper.TossMapper">
    <insert id="confirmPayment" parameterType="TossVO">
        INSERT INTO Toss_t(u_idx, id, orderId, orderName, amount, approvedAt, dueDate)
        VALUES(#{u_idx}, #{id}, #{orderId}, #{orderName}, #{amount}, #{approvedAt}, #{dueDate})
    </insert>
</mapper>
