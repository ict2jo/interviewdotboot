<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ict.interviewdotboot.mapper.ReviewMapper">
    <select id="getReviewList" resultType="ReviewVO">
        SELECT * FROM Review_t ORDER BY r_idx 
    </select>
    <select id="getReviewDetail" resultType="ReviewVO">
        SELECT * FROM Review_t WHERE r_idx = #{r_idx}
    </select>
    <insert id="insertReview" parameterType="ReviewVO">
        INSERT INTO Review_t(r_id, r_title, r_content, r_company, r_regdate)
        VALUES(#{r_id}, #{r_title}, #{r_content}, #{r_company}, sysdate())
    </insert>
    <update id="updateReview" parameterType="ReviewVO">
        UPDATE Review_t
        SET r_content = #{r_content}
        WHERE r_idx = #{r_idx}
    </update>
    <update id="deleteReview" parameterType="ReviewVO">
        UPDATE Review_t
        SET active = #{active}
        WHERE r_idx = #{r_idx}
    </update>
</mapper>